extends _template

block preheader
  | Change your #{config.siteName} password.

block body
  h1(style="font-size: 24px;margin:0 0 20px 0") Hi #{user.shortName},

  p You recently requested to change the password for your #{config.siteName} account. Please click on the following link to choose a new password:

  +button(`https://#{config.domain}/reset/${user.passwordResetToken}`, "Reset Password")

  p You can also copy and paste this URL into your browser: https://#{config.domain}/reset/#{user.passwordResetToken}

  p If you did not request to change your password, please ignore this email or <a href="mailto:#{config.accounts.supportEmail}">let us know</a>. The reset link above is valid for one hour.
